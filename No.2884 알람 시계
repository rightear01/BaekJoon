#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*최대의 시간은 23시 59분이다. 따라서 시간(0 <= H < 24)이며, 분(0 <= M < 59)라는 것을 인지하고 있어야 한다. 
필자는 앞당긴 시간을 구하는 문제이다 보니까 처음부터 45분을 빼고 케이스를 구분하였다.

첫 번째 케이스 (H != 0 && M < 0) -> 입력받은 시간이 0시가 아니고, 45분을 뺀 값이 0 미만일 때
1시에서 1을 빼더라도 최소 0시이기 때문에, 시간에서 –1, 분에서 +60을 하여, 분을 계산하였다.
예시)
H = 9
M => 10 - 45 = -35 ( M < 0 )
-> M(10) - 45 + 60 = 25
-> H - 1 = 9

두 번째 케이스는 (H == 0 && M < 0) 시간이 0시이면서 분은 0 미만일 경우이다. 
이 경우에는 23시로 시간을 변경 시켜 주어야 하기 때문에 H = 23, M에는 +60을 해주었다.
세 번째 케이스는 H가 0이 아니고 M도 0 이상이므로 그냥 출력하면 된다.

전반적으로 시간에 대한 개념을 이용하여 문제에 접근하면 되는 것이다.*/

int main(){
	int H, M;
	scanf("%d %d", &H, &M);

	M -= 45;
	if (H != 0 && M < 0){
		H -= 1;
		M += 60;
		printf("%d %d\n", H, M);
	}

	else if (H == 0 && M < 0){
		H = 23;
		M += 60;
		printf("%d %d\n", H, M);
	}

	else{ printf("%d %d\n", H, M);}

	return 0;
}
