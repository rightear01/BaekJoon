#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int numA, numB, repetitions;
	scanf("%d", &repetitions);

	for (int i = 0; i < repetitions; i++)
	{
		scanf("%d %d", &numA, &numB);
		printf("Case #%d: %d\n", i + 1,  numA + numB);
	}

	return 0;
}
