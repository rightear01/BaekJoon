#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

// 100 * 100 크기의 정사각형 행렬이 있는데 10 * 10 사이즈의
// 정사각형 색종이가 최대 100개 들어갈 수 있다.
// 색종이는 겹칠 수 있고 100 * 100 사이즈를 넘어 그 밖으로 삐져나갈 수 없다.
// 색종이들이 차지하는 넓이를 구하는 문제이다.

/*
접근법
입력값으로 받는 값은 열의 시작 좌표와 끝 행의 좌표이다.
따라서 필자는 이 점을 이용하였다.
열의 시작 좌표 + 9는 가로 사이즈
99(배열의 100번지는 널 값)부터 90 - (주어지는 두번째 값)은 세로 사이즈를 의미하게 된다.
또한 방 하나의 크기는 넓이 1과 동일하다.
따라서
100 * 100 사이즈의 0 행렬을 만들고
입력값을 기점으로 가로, 세로 사이즈 만큼을 1로 변환하여 채운다.
이후에 들어오는 값들 중에서 겹치는 값들은 카운트 변수로 채크하고
색종이의 수(testCase) * 100 = 겹치지 않을때의 넓의의 합
겹치지 않을때의 넓의의 합 - 누적된 카운트(넓이 1의 정사각형들의 합)
= 색종이들이 차지한 최종 넓이가 되는 것이다.
*/

int main(){
	int array[100][100] = { 0, };
	int x, y, testCase, ctr = 0;
	scanf("%d", &testCase);

	for (int i = 0; i < testCase; i++){
		scanf("%d %d", &x, &y);
        for (int i = 99 - y; i >= 90 - y; i--)
	        for (int j = x; j < x + 10; j++){
				if (array[i][j] == 0) { array[i][j] = 1; }
				else { ctr++;  continue; }
			}
	}
	printf("%d", testCase * 100 - ctr);
	return 0;
}
